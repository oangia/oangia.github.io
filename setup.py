from setuptools import setup

setup(
    name='oangia',
    version='0.1',
    install_requires=[
        'pydub',
        'gTTS',
        'SpeechRecognition',
        "moviepy==1.0.3",
        "gspread",
        "pandas"
    ]
)
